apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: admin-service-config
spec:
  features:
    - name: SchedulerReminders
      enabled: false
  tracing:
    samplingRate: '0'
  mtls:
    enabled: false
