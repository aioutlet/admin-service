# Admin Service - Configuration Template
# Copy this file to .env and customize for your environment

# ===========================================
# SERVER CONFIGURATION
# ===========================================
PORT=3010
HOST=localhost
NODE_ENV=development
API_VERSION=1.0.0
SERVICE_NAME=admin-service

# ===========================================
# AUTHENTICATION CONFIGURATION
# ===========================================
JWT_SECRET=your-super-secret-jwt-signing-key-at-least-32-characters-long
JWT_ALGORITHM=HS256
JWT_EXPIRES_IN=8h
JWT_ISSUER=aioutlet-auth-service
JWT_AUDIENCE=aioutlet-admin-service

# ===========================================
# ADMIN SECURITY CONFIGURATION
# ===========================================
ADMIN_SESSION_TIMEOUT=28800000
ADMIN_MAX_LOGIN_ATTEMPTS=10
ADMIN_LOCKOUT_DURATION=300000
ADMIN_PASSWORD_MIN_LENGTH=8

# ===========================================
# CORS CONFIGURATION
# ===========================================
CORS_ORIGIN=http://localhost:3000
CORS_METHODS=GET,POST,PUT,DELETE
CORS_ALLOWED_HEADERS=Content-Type,Authorization
CORS_CREDENTIALS=true

# ===========================================
# RATE LIMITING
# ===========================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_MESSAGE=Too many requests, please try again later

# ===========================================
# LOGGING CONFIGURATION
# ===========================================
LOG_LEVEL=info
LOG_FORMAT=combined
LOG_TIMESTAMP=true

# ===========================================
# EXTERNAL SERVICE INTEGRATION
# ===========================================
# Admin service connects to other microservices for management operations
# Currently only USER_SERVICE is actively used in the codebase
USER_SERVICE_URL=http://localhost:3002/api/admin/users
USER_SERVICE_SECRET=your-shared-secret-key

# Future service integrations (add as needed when implemented):
# AUTH_SERVICE_URL=http://localhost:3001/api/admin/auth
# PRODUCT_SERVICE_URL=http://localhost:3003/api/admin/products
# INVENTORY_SERVICE_URL=http://localhost:3004/api/admin/inventory
# ORDER_SERVICE_URL=http://localhost:5000/api/admin/orders
# PAYMENT_SERVICE_URL=http://localhost:7000/api/admin/payments
# NOTIFICATION_SERVICE_URL=http://localhost:3008/api/admin/notifications
# AUDIT_SERVICE_URL=http://localhost:9000/api/admin/audits

# ===========================================
# HEALTH CHECK CONFIGURATION
# ===========================================
HEALTH_CHECK_ENDPOINT=/health
HEALTH_CHECK_TIMEOUT=5000

# ===========================================
# SECURITY NOTES:
# ===========================================
# 1. Admin service does not have its own database
# 2. All data operations are performed via service APIs
# 3. Use different credentials for each environment
# 4. In production, use environment variables or secrets management
# 5. Rotate passwords and JWT secrets regularly

# ===========================================
# ENVIRONMENT FILES:
# ===========================================
# .env              - Default environment (copy from .env.example)
# .env.example      - This template file
# .env.development  - Development-specific settings
# .env.production   - Production-specific settings
