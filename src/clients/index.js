/**
 * External HTTP Clients
 * This folder is for HTTP clients that make calls to external services
 * (e.g., other microservices, third-party APIs)
 * 
 * Infrastructure concerns like Dapr and secret management belong in src/core/
 */

export { userServiceClient } from './user.service.client.js';
